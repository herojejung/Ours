<h1>サブカテゴリー一覧</h1>

<table>
  <thead>
    <tr>
      <th>サブカテゴリー名</th>
      <th>カテゴリー名</th>
    </tr>
  </thead>
  <tbody>
    <% @sub_categories.each do |sub_category| %>
      <tr>
        <td><%= sub_category.name %></td>
        <td><%= sub_category.category.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>