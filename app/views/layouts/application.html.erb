<!DOCTYPE html>
<html>
  <head>
    <title>ours</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://kit.fontawesome.com/f7f13ef182.js" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

<body>
  <header class="<%= (admin_signed_in? ? "admin_header" : "" ) %>">
    <div class="header-content">
      <%= link_to 'Ours', (admin_signed_in? ? admin_root_path : user_root_path ), class: "logo" %>
    <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container">
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav ml-auto">
          <% if admin_signed_in? %>
            <li class="nav-item">
              <%= link_to admin_root_path do %>
                <i class="fa-solid fa-camera">投稿一覧</i>
              <% end %>
            </li>
            <li class="nav-item">
              <%= link_to admin_users_path do %>
                <i class="fa-regular fa-circle-user">会員一覧</i>
              <% end %>
            </li>
            <li class="nav-item">
              <%= link_to destroy_admin_session_path, method: :delete do %>
              <i class="fa-solid fa-arrow-right-from-bracket">ログアウト</i>
              <% end %>
            </li>
          <% elsif user_signed_in? %>
            <li class="nav-item">
              <%= link_to user_root_path do %>
              <i class="fa-solid fa-house">HOME</i>
            <% end %>
            </li>
            <li class="nav-item">
              <%= link_to destroy_user_session_path, method: :delete do %>
              <i class="fa-solid fa-arrow-right-from-bracket">ログアウト</i>
              <% end %>
            </li>
             <li class="nav-item">
               <%= link_to　user_user_path(current_user.id) do %>
               <i class="fa-regular fa-circle-user">マイページ</i>
               <% end %>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to new_user_registration_path do %>
              <i class="fa-solid fa-user-plus">新規登録</i>
              <% end %>
            </li>
            <li class="nav-item">
              <%= link_to new_user_session_path do%>
              <i class="fa-solid fa-arrow-right-to-bracket">ログイン</i>
              <% end %>
            </li>
          <% end %>
        </ul>
      </div>
      </div>
      </nav>
    </header>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
    <main>
      <%= yield %>
    </main>
    <footer>Copyright © Infratop Inc. All Rights Reserved.</footer>
  </body>
</html>
