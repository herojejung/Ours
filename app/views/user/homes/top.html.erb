<%= render 'user/homes/header' %>
 <div class ="container">
    <div class ="row">
      <div class="col-sm">
          <% @post_images.each do |post_image| %>
          <li><%= image_tag(post_image.images.first) %></li>
          <li><%= post_image.title %></li>
          <li><%= post_image.text %></li>
        <% end %>
        </div>
      </div>
     </div>