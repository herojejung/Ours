<h1><%= @category.name %></h1>
<ul id="sub_categories_list">
  <% @sub_categories.each do |sub_category| %>
    <li class="sub_category-item" style="list-style: none;"><%= link_to sub_category.name, user_root_path(sub_category_id: sub_category.id) %></li>
  <% end %>
</ul>
